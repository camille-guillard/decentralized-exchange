const dexAbi = require("./dex-abi.json");
const poolAbi = require("./pool-abi.json");
const tokenAbi = require("./token-abi.json");
const dexAddresses = require("./dex-addresses.json");

module.exports = {
    dexAbi,
    poolAbi,
    tokenAbi,
    dexAddresses
}